var PROXIMITY = PROXIMITY || {};

PROXIMITY.Zone = (function() {
	'use strict';

	var Zone = function Zone(uuid, name) {

		this.uuid = uuid;
		this.name = name;
		this.beaconCount = 0;

	};

	App.prototype.updateDom = function() {
		//for each child object that is a beacon update its position on the radius map
	};

	return Zone;

})();